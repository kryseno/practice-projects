$(document).ready(initializeApp);

function initializeApp(){
    $("div").click(handleDivClick);
}

function handleDivClick(){
    var lastListItem = 
    console.log('div clicked');
}

// 1. make both divs clickable
// 2. save last li into var
// 3. append that li into ul that DOES NOT have class (this class)